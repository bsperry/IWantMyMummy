@model BurialInformationViewModel
@*@model IWantMyMummy.Models.BurialQuadrant*@

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container viewContainer">
    <h1>Create Burial Quadrant</h1>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="@Model.BurialQuadrant.BurialSquareId" class="control-label">Burial Square ID</label>
                    <select asp-for="@Model.BurialQuadrant.BurialSquareId" class="form-control">
                        @foreach (var SquareID in Model.BurialSquare)
                        {
                            <option>@SquareID.BurialSquareId</option>
                        }
                        <br />

                    </select>
                    <p>
                        <a class="btn btn-primary" asp-controller="BurialSquares" asp-action="Create">Create New Burial Square</a>
                    </p>

                    <span asp-validation-for="@Model.BurialQuadrant.BurialSquareId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.BurialQuadrant.BurialSubplot" class="control-label">Burial Quadrant</label>
                    <select asp-for="@Model.BurialQuadrant.BurialSubplot" class="form-control">
                        <option></option>
                        <option>NE</option>
                        <option>NW</option>
                        <option>SE</option>
                        <option>SW</option>
                    </select>

                    @*@foreach (var subid in Model.BurialQuadrantsList)
                        {
                            if (@subid.BurialSubplot != @ViewBag.AvailPlots[0])
                            {
                                if (Model.Subplots.Contains(subid.BurialSubplot) == false)
                                {
                                    Model.Subplots.Add(@subid.BurialSubplot);
                                }
                            }
                            if (@subid.BurialSubplot != @ViewBag.AvailPlots[1])
                            {
                                if (Model.Subplots.Contains(subid.BurialSubplot) == false)
                                {
                                    Model.Subplots.Add(@subid.BurialSubplot);
                                }
                            }
                            if (@subid.BurialSubplot != @ViewBag.AvailPlots[2])
                            {
                                if (Model.Subplots.Contains(subid.BurialSubplot) == false)
                                {
                                    Model.Subplots.Add(@subid.BurialSubplot);
                                }
                            }
                            if (@subid.BurialSubplot != @ViewBag.AvailPlots[3])
                            {
                                if (Model.Subplots.Contains(subid.BurialSubplot) == false)
                                {
                                    Model.Subplots.Add(@subid.BurialSubplot);
                                }
                            }
                        }
                        @foreach (var subid in Model.BurialQuadrant.BurialSubplot)
                        {
                            <option>@subid</option>
                        }</select>*@


                    <span asp-validation-for="@Model.BurialQuadrant.BurialSubplot" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create Burial Quadrant" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-action="Index">Back to Quadrants</a>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
